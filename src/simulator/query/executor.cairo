mod utils;

use cairo_db_simulator::simulator::query::executor::utils::query_parser;
use cairo_db_simulator::simulator::state::state::State;
// // parses the queries and executes sequentially
// fn execute_all(state: State, queries: Array<felt252>) -> State {}

// // executes one query at a time
// fn execute_one(state: State, query: felt252) -> State {}

